PROPERTY: header-args:emacs-lisp :tangle init-solidity.el :lexical t
#+TITLE: init-solidity.el

* init-solidity.el 文件头
#+begin_src emacs-lisp :tangle yes
  ;; init-solidity.el --- Initialize solidity configurations. -*- lexical-binding: t -*-
  ;;
  ;;; Commentary:

  ;;; Code:
#+end_src

* solidity-mode
#+begin_src emacs-lisp :tangle yes
  (use-package solidity-mode
    :hook (solidity-mode . (lambda ()
                             (c-set-offset 'defun-block-intro 4)
                             (c-set-offset 'statement-block-intro 4)))
    :config
    (setq solidity-comment-style 'slash))
#+end_src

* init-solidity.el 文件尾
#+begin_src emacs-lisp :tangle yes

  (provide 'init-solidity)
  ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
  ;;; init-solidity.el ends here
#+end_src
